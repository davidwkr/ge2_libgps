# Deep Vendor CP Force Fix

We are moving to a low-level vendor configuration strategy to overcome the persistent Framework lock on the "User Plane" path.

## Proposed Changes

### [NEW] [revert_to_stock_configs.sh](file:///home/latte/Documents/repos/ge2_libgps/revert_to_stock_configs.sh)
- **Stock Restore**: Overwrite `/vendor/etc/config.xml` and `/vendor/etc/supl.xml` with files from `stock_configs/`.
- **Cache Purge**: Remove all persistent GNSS data in `/data/gnss/lte/` and session data in `/data/gnss/supl/` to ensure a factory-clean start.

## Verification Plan
1. Run script and **REBOOT**.
2. Verify `cat /vendor/etc/config.xml` does not contain `CP-ENABLE`.
3. Verify `cat /vendor/etc/supl.xml` points to default carrier/vendor servers.
